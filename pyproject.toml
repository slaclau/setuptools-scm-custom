[project]
name = "setuptools-scm-custom"
dynamic = ["version"]
dependencies = ["setuptools>=64", "setuptools_scm>=8"]

[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]